/// アプリケーションのテキストを表す構造体
#[derive(Default)]
pub struct AppText {
    pub header: String,
    pub footer: String,
    pub help: String,
    pub disp: String,
    pub result: String,
    pub win_bonus: String,
}
